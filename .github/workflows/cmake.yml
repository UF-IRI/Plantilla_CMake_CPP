name: Linux Build

on: [push]

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v1
    - name: build and run
      run: |
        cmake .. -DCMAKE_BUILD_TYPE=Release
        make -j
        ctest
      
