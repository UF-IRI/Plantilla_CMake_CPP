name: Windows Build

on: [push]

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v1
    - name: build and run
      run: |
        cmake -G "Visual Studio 17 2022" -A x64
        cmake --build build
        cd build
        ctest
      
